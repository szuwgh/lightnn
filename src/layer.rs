struct Network {
    num_layers: usize,
    sizes: Vec<usize>,
}

impl Network {
    fn new() -> Network {
        Self {}
    }

    fn feedforward() {
        //Return the output of the network if ``a`` is input.
    }

    fn SGD() {}
}

fn sigmoid(z: f64) {}
